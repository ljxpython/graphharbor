# 验收方案与证据

**项目状态：`partial`（2026-09-25）。** 功能与隔离 PG17 容量验证已有结果；官方差分、平台 API 联合安全链路、清理/订阅竞态及稳定容量/回退证据尚缺，不可宣称全面兼容或上线完成。完成门槛是 GraphHarbor、runtime-service 和平台 API 的 HTTP/worker/数据库链路；浏览器烟测只是补充。数据库验证只使用 `graphharbor_event_retention_test`、`graphharbor_event_retention_verify`、`graphharbor_event_retention_restore` 和新建的 `graphharbor_event_retention_candidate`，未运行会清空默认库的 `scripts/test.sh`。

2026-09-26 定向复测：显式设置隔离 `DATABASE_URI` 与 `REDIS_URI` 后，事件保留、官方 SDK 契约、生产 worker 和应用授权为 102 passed、4 skipped；Ruff 通过。平台 runtime-gateway 为 68 tests、2 skipped，Web chat session 为 20 passed、1 skipped。首次省略隔离 URI 的命令连接默认 `langgraph` 失败，未计入结果；项目仍为 partial。

同日旧归档只读组成分析：PG17 独立恢复库 `graphharbor_boundary_restore_post33` 的 `runtime_events` 共 434,742 行；messages、debug、checkpoints 分别占 399,549 / 18,387 / 6,277 行，JSONB payload 大小分别为 4.06 / 1.87 / 1.01 GB。其余 topic 及精确字节数见 `tasks.md`。未读取正文，不代表新版本稳态容量。

| 场景 | 预期与实际证据 | 状态 |
| --- | --- | --- |
| V01 事实基线 | `measure_event_storage.py` 走真实 `record_message_deltas`；同一 Run 10,000 条增量，payload 6,540,000 B、heap 8,265,728 B、TOAST 32,768 B、索引 1,687,552 B，写入 2.557s；停维护后的等量重写 2.623s。未含端到端 SSE 延迟；旧归档未作 topic 细分。 | 部分通过 |
| V02 官方契约 | 锁定 `langgraph-api 0.13.0` / `langgraph-sdk 0.4.3` 的本机 `langgraph dev`：可恢复流在进程存活时可回放，重启后 Run/Thread/Protocol 均不回放但资源仍 200；完成 Run 无 `stream_resumable` 时三个入口均不回放。运行中/错误初探发现官方 `POST /runs` 200、GraphHarbor 201，官方错误流有 `error` 帧、GraphHarbor v2 无此帧；候选 wheel PG 第 18 条终态事件的 `status=error` 且含错误载荷，从游标 0 重连的 v2 SSE 仍只有 `metadata`、`values`，确认映射差异。仍缺鉴权及超窗逐项差分。 | 部分通过 |
| V03 清理正确性 | 隔离 PG17 `test_event_retention.py` 验证 1,000 行批次、并发维护、旧/新、成功/失败终态/运行中、无 Run 事件、水位及幂等；失败终态错误载荷、Run checkpoint baseline 和 Store 记录在清理后不变。10,000 条旧终态增量经 10 批 0.398s 清零，Run/Thread 水位 10000；迁移 009 降至 008 再升至 009 成功。另以两个真实 worker 进程对 10,000 条旧增量竞争维护，原始行清零、唯一终态与水位保留。真实 HITL/重试期间清理和清理/订阅竞态尚未专项验收。 | 部分通过 |
| V04 流式恢复 | 隔离库定向测试验证 Run/Thread/Protocol 旧游标报 `cursor_expired`，Protocol 返回顶层 `code` 的 HTTP 410；Redis 扇出异常注入测试确认 PG 事件仍可回放；平台流式 HTTP 适配器测试验证 410 与 `recovery` 保留。真实独立 Redis 断连复现 worker 错误取消，修复后候选 API/worker 上的 15 秒 Run 跨约 5.5 秒断连成功，PG 终态唯一，三个流入口恢复回放。清理/订阅竞态、平台 API 联合撤权、JS SDK 尚未验收。 | 部分通过 |
| V05 安全边界 | 过期游标与拒绝读取的交叉回归验证 Run/Thread/Protocol 均先返回 403，不泄露水位；`graphharbor_boundary_event_retention` 隔离库的应用授权测试 25 passed。新增 Runtime 直连回归：Alice 获权旧游标可收到过期响应，撤权后三个入口均 403；Bob 通过 ACL 回调但不满足 owner 过滤，Run/Protocol 为 404、Thread 为 404 错误帧。平台 API 联合撤权及跨项目旧游标仍缺。 | 部分通过 |
| V06 稳定容量 | 清理后目标 Run 活跃原始行 0，隔离库事件总行数 57；`runtime_events` 表总大小仍约 20 MB，`n_dead_tup=20000`（包含测试 UPDATE/DELETE），说明 `DELETE` 不即时缩文件。单次维护上限 20 批；尚无长期稳态趋势、Redis 内存、写入 p95 或线上预算判定。 | 部分通过 |
| V07 平台联合 | 平台 39 项 `unittest` 通过，涵盖网关、SDK 适配和 410 错误传递；Web `useChatSession.spec.ts` 20 passed，真实 `sdk-chain.test.ts` 1 skipped。旧 `dist/` runtime wheel 缺迁移 009，不能作候选；当前源码双包 0.13.0.post32 已重新构建、独立安装并从安装目录导入。独立 PG17 候选库迁移到 009，在 18631 端口启动候选 API/worker 后，确定性图的 Run 初始/重连、Thread 全量/续传和 Protocol 返回帧；启动 worker 前探针只收到 metadata 并超时，不能算通过。另建隔离平台库迁移到 `20260925_0005`，候选 Runtime API/worker 与平台 API 在 18632/18633 联调；补齐 `PLATFORM_THREAD_AUTHORIZATION_URL` 后，网关 Thread 创建、读取、state/history 返回 200，未登录读取 401。平台委托角色修复后 Graph 搜索 200，返回 4 个图；相关整文件 3 项单测通过。Web 18634 浏览器登录、会话治理读取、Graph 目录刷新可用；断线续传、HITL 和文件链路未运行，浏览器仅为补充证据。真实 `miaomiaoai` 模型仅在 GraphHarbor 验收图通过，平台 runtime-service 的业务模型策略仍待独立验收。 | 部分通过 |
| V08 回退 | PG17 备份 `/tmp/graphharbor-event-retention-precleanup-20260925.dump` 已完整恢复到独立库 `graphharbor_event_retention_restore`，恢复库保留 10,000 条旧原始事件；停维护后新写入 10,000 条成功。已删事件不得直接覆盖在线库恢复；未演练按 ID 离线提取与合并。 | 部分通过 |

2026-09-25 定向命令与结果：`DATABASE_URI=postgresql+asyncpg://localhost/graphharbor_event_retention_verify REDIS_URI=redis://localhost:6379/15 BOUNDARY_TEST_DATABASE_URI=postgresql+asyncpg://localhost/graphharbor_boundary_event_retention uv run --no-sync pytest -q libs/langgraph-runtime-pg/tests/test_event_retention.py libs/langgraph-runtime-pg/tests/test_official_sdk_contract.py libs/langgraph-runtime-pg/tests/test_production_contract.py libs/langhost/tests/test_application_authorization.py` 为 99 passed、4 skipped；独立重跑新增/更新后的 `test_event_retention.py` 为 4 passed。平台使用 `apps/platform-api/.venv/bin/python -m unittest discover -s tests -p <目标文件>`，三个目标文件共 39 项通过；Web `pnpm exec vitest run src/modules/chat/composables/useChatSession.spec.ts src/modules/chat/sdk-chain.test.ts` 为 20 passed、1 skipped。`uv run --no-sync ruff check libs/langgraph-runtime-pg/tests/test_event_retention.py` 与 GraphHarbor `git diff --check` 通过。跳过项不计通过。

2026-09-25 本轮补验：同一 `graphharbor_event_retention_verify` 隔离库重跑 `test_event_retention.py` 为 5 passed（新增失败终态参数）；`uv run --no-sync ruff check libs/langgraph-runtime-pg/tests/test_event_retention.py` 和 `git diff --check` 通过。候选安装目录为 `/tmp/graphharbor-event-retention-candidate/install`，从该目录导入 `langhost`、`langgraph_runtime_pg` 得版本均为 0.13.0.post32，清理方法与迁移 009 存在。新建 `graphharbor_event_retention_candidate`，只对该库执行 `graphharbor migrate upgrade`，输出 009；候选 API 端口 18631、worker 使用 Redis DB 14 和独立前缀。`probe_event_replay.py --url http://127.0.0.1:18631` 在 worker 启动后取得 Run/Thread/Protocol 回放；第一次 worker 未启动时超时，已保留为非通过证据。停机时打开中的 Protocol 长连接触发 asyncpg 连接取消日志，未见数据丢失，但正常断线恢复仍未验收。

2026-09-25 本次验收口径调整后，隔离 PG17 四文件定向回归重跑两次，均为 100 passed、4 skipped；新增的 Runtime 直连 ACL 动态撤权及跨用户 owner 过滤断言所在单文件为 5 passed。`uv run --no-sync ruff check libs/langgraph-runtime-pg/tests/test_event_retention.py` 和 `git diff --check` 均通过。浏览器断线、HITL 和文件操作不再作为事件保留专项的完成门槛；现有浏览器烟测只保留为补充证据。

2026-09-25 Redis 故障复测：独立 Redis 18635（无持久化）、候选 Runtime PG17 库、API 18636 和 worker 使用相同独立前缀。第一次 worker 命令误用 `-c`，Run 停在 pending，该次不计通过。正确启动旧 wheel 后，30 秒确定性 `v3_slow` Run 在 Redis 中断后成为 `interrupted/cancel_requested`，PG 保留 8 条事件及终态；源码定位到 worker 的 Redis 心跳异常触发 `cancel_event`。新增 `test_redis_heartbeat_outage_does_not_cancel_postgres_owned_run` 后，修复前该用例失败；修复后四文件回归 101 passed、4 skipped，Ruff 与 `git diff --check` 通过。重新从当前源码构建 runtime wheel，和既有 langhost wheel 安装到 `/tmp/graphharbor-event-retention-fixed/install`，确认实际导入修复版。worker 设 6 秒 PG lease，使心跳每约 2 秒执行；15 秒确定性 Run 运行中停止 Redis 约 5.5 秒后重启，最终 API/PG 均为 `success/completed`，PG `runtime_events` 13 条、其中终态 1 条。`probe_event_replay.py --thread ... --run ... --last-id 0 --since 0` 返回资源 200，Run `values` 帧、Thread 13 帧及 Protocol 2 条 lifecycle 帧；后两个长连接读到目标帧后因探针 3 秒窗口超时，不能据此推断流终止语义。临时 API/worker/Redis 进程均已关闭，18635/18636 无监听。

2026-09-25 多进程清理复测：在 `graphharbor_event_retention_candidate` 用 `measure_event_storage.py` 写入 10,000 条确定性增量，单次写入 3.002s，payload 6,540,000 B；仅对该 fixture Run 设旧时间和成功终态，并增加 1 条终态事件。两个从修复版 wheel 启动的独立 worker 进程同时使用 Redis DB 14 的独立前缀、`GRAPHHARBOR_REAPER_INTERVAL_SECONDS=0.5`、24 小时保留与 1,000 行批次。事前原始/终态为 10000/1；事后为 0/1，Run 和 Thread 水位均为 10000，Run 仍为 success。两个进程均已关闭；当前候选库 `runtime_events` 总活跃行 105、表总大小 18,874,368 B，不能用表文件未立刻缩小推断长期趋势。此探针证明进程级并发结果，未证明两个 worker 各自处理了多少批次。

2026-09-25 真实模型烟测：固定候选双 wheel 在隔离 PG17 库、Redis DB 14 独立前缀、API 18636 和 worker 上加载仓库验收图 `chat`。仅从 `~/.my_best/.env` 向临时进程注入 `MIAOMIAOAI_PROXY_URL`、`MIAOMIAOAI_PROXY_API_KEY`，模型名 `deepseek-v4.1-flash`；本记录不含 URL、密钥或对话正文。Run API 为 success，Thread state 的 `provider_streamed=true`、`token_count=2` 且响应非空；PG 该 Run 事件 13 条，其中终态 1 条。此证据只覆盖 GraphHarbor 通用执行，不证明平台 runtime-service 的模型配置、ACL 或业务工具策略。临时 API/worker 已停止。

同一候选栈另用 `v3_edge` 的 `scenario=failed` 创建 Thread/Run：创建响应 201，最终 Run 为 `error`；PG `runtime_events` 第 18 条为 `terminal=true` 的 `lifecycle/error`，载荷含错误字段；从 `Last-Event-ID: 0` 读取 v2 Run SSE 返回 200，只含 `metadata`、`values`，没有官方的 `error`。Thread lifecycle 的同步整包读取因长连接超时，该次探针无判定价值；后续需逐帧消费。两次候选 API/worker 临时进程均已停止。

独立平台候选库 `graphharbor_event_retention_platform_candidate` 使用 `PLATFORM_API_PLATFORM_DB_ENABLED=true`、`PLATFORM_API_PLATFORM_DB_AUTO_CREATE=false` 执行平台 Alembic `upgrade` 到 `20260925_0005`；该库和 Runtime 候选库均不接默认平台栈。平台 API 18633、候选 Runtime API/worker 18632 的 `/health` 与 `/ready` 为 200；最初 Thread 创建 500，因 Runtime 缺 `PLATFORM_THREAD_AUTHORIZATION_URL`，修正隔离进程环境后重测创建/读取/state/history 均 200，未登录读取 401。平台 `scripts/local-stack.sh` 已补同一配置，`bash -n` 与 `git diff --check` 通过；脚本默认 `start` 会同步旧锁文件，未对现有栈执行。首次 `graphs/search` 403，因平台管理员的委托被项目角色覆盖；平台网关改为优先传平台角色，新增角色分支单测并整文件 3 tests 通过，隔离 HTTP 重测 Graph 搜索 200、4 个图，Thread 创建仍 200。普通项目角色不获得平台级委托。平台 Web 等剩余链路不能据此算通过。

Web 浏览器烟测使用同一隔离双库与端口 18632/18633，另以 18634 启动 Vite：`playwright-cli` 登录后进入项目会话治理，输入候选 Thread ID 并定位，页面显示“会话已读取，暂无消息或待审批项”；页面刷新后登录与项目上下文仍在，但定位输入未自动保留，故未把刷新记为会话续传通过。Graph 页面点击“刷新目录”后显示 `workflow_demo`；所见浏览器控制台为 0 errors，Vite `vue-tsc` 为 0 errors。浏览器及三个临时服务已停止；无模型调用、未进行断线/HITL/文件操作。

下一步完成 V02 的官方超窗/鉴权差分和 V05 的平台 API 联合撤权/跨项目差分；在隔离候选双包与平台 API 栈验证 HTTP 断线恢复、Run 状态/历史、清理/订阅竞态、稳定期容量和离线回退合并。真实模型 `miaomiaoai` 的通用图烟测已完成，平台业务模型策略仍属平台侧验收；容量、清理和故障使用确定性图。现有浏览器烟测保留为补充证据，不要求浏览器断线/HITL/文件操作。当前平台服务监听本机业务数据库，不作为隔离候选验收。未补齐前 T02、T04、T05 保持未完成。

2026-09-25 后续实施记录：针对已知官方差异，Run 创建响应改为 200，v2 Run SSE 从持久化失败/超时终态输出 `error` 帧；对创建时未启用 `stream_resumable` 的 Run，已有 Run 的 SSE 订阅不回放历史。新增失败帧与非续传订阅回归。`test_event_retention.py` 串行重跑 7 passed，`test_rest_contract.py -k run` 3 passed，涉及文件 Ruff check 通过。首次将两组写入同一隔离 PG17 库的测试并行执行，导致事件测试偶发 `RunOwnershipError`；该次失败不计通过，停止并行后复测成功。上述仅覆盖 Run SSE 局部行为；Thread/Protocol 非续传行为及候选 HTTP/SDK 差分尚待验证，T02 保持未完成。

同日补验：锁定的官方 `langgraph-api 0.13.0` 本机 HTTP 实测 `POST /runs` 为 200、失败 v2 SSE 输出 `error` 帧，未启用 `stream_resumable` 的完成 Run 在 Run/Thread/Protocol 三入口均无历史帧。GraphHarbor 候选源码 API/worker 在独立 PG17 库 `graphharbor_event_retention_candidate` 的错误流已输出相同 `ValueError` 帧；非续传 Thread/Protocol 重连无历史帧。Run 重连初测多出一帧 metadata，已修复并由隔离 PG17 回归覆盖。6 个定向文件串行重跑 109 passed、4 skipped；首次运行有 4 项旧 fixture 在未设置续传时仍断言回放，已按官方语义修正后复测通过。版本提升至双包 `0.13.0.post33` 供后续发布和平台锁定；这不是全入口官方差分通过的结论。
