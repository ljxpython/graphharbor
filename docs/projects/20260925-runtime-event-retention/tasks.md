# 实施任务

实施中；各项状态以本清单和 `verification.md` 的实测记录为准。

- [x] **T01 基线与消费者清单：** 位置：`tests/acceptance_app/measure_event_storage.py`、`run_store.py`、平台 runtime_gateway；预期：脱敏容量与消费者清单。2026-09-25 在隔离 PG17 库 `graphharbor_event_retention_test` 运行 `DATABASE_URI=... REDIS_URI=... uv run python tests/acceptance_app/measure_event_storage.py`：10,000 条同一 Run 增量，JSONB payload 6,540,000 B、heap 8,265,728 B、TOAST 32,768 B、索引 1,687,552 B、写入 2.557s；复查 topic 计数和单 Run 峰值均为 10,000。该 fixture 走真实 `record_message_deltas`，不含 worker/SSE 计时。Redis 默认 `MAXLEN=2000`、终态 TTL 至少 3600s；平台网关 `stream_resumable=true`、Protocol 浏览器订阅、Run SDK adapter 均已核对。旧归档未恢复，存量组成仍未知。
- [ ] **T02 官方回放契约：** 位置：`tests/acceptance_app/probe_event_replay.py`、官方/GraphHarbor 流入口；预期：各入口回放、过期、错误和鉴权差分可复现。2026-09-25 已用同一 `v3_edge` 图实测 `stream_resumable=true`、Run join、Thread `"-"`/ID、Protocol `since=0` 和官方进程重启；另以 `--no-resumable` 实测官方完成 Run 的 Run/Thread/Protocol 均不回放，而 GraphHarbor 仍回放。运行中/错误 Run 初探发现官方 `POST /runs` 200、GraphHarbor 201，以及官方错误流有 `error` 帧而 GraphHarbor v2 无该帧；隔离候选 wheel 错误 Run 的 PG 终态和从 0 重连的 v2 SSE 已复核该差异。仍待超窗和鉴权差分；状态 **partial**。
- [x] **T03 有界事件保留：** 位置：`run_store.py`、`production_worker.py`、`models.py` 和迁移 009；预期：仅清除过期终态 Run 的非终态行，单事务最多 1,000 行、每轮最多 20 批，水位可重启恢复。2026-09-25 隔离 PG17 回归覆盖旧/新、成功/失败终态/运行中、无 Run、两个并发维护调用，以及失败终态错误载荷、Run checkpoint baseline 与 Store 保留；迁移 009 降至 008 再升回 009 成功。独立候选库补测：确定性 fixture 写入 10,000 条原始事件与 1 条终态，两个独立 worker 进程用同一 Redis 前缀、500ms reaper 间隔同时运行；清理后原始事件为 0、终态仍为 1、Run/Thread 水位均为 10000，进程均正常停止。四文件定向回归 101 passed、4 skipped；2026-09-25 完成。
- [ ] **T04 回放与过期结果：** 位置：`streaming.py`、`protocol_api.py`、`production_worker.py`；预期：明确旧游标报 `cursor_expired`，新订阅允许从保留事件开始，平台透传 Protocol 410，Redis 故障不把仍由 PG lease 持有的 Run 误判为取消。2026-09-25 隔离 PG17 回归断言 Run/Thread/Protocol 三入口旧游标和授权拒绝先于水位读取；Runtime 直连的 Alice 获权后撤权返回 403、Bob 的 owner 不匹配不能读取三个旧游标入口。真实独立 Redis 断连曾使 30 秒确定性 Run 变成 `interrupted/cancel_requested`；新增失败回归后，生产 worker 将 Redis 心跳作为辅助信号，修复版 wheel 在独立 Redis 停止约 5.5 秒后重启的 15 秒 Run 最终为 `success/completed`，PG 保留 13 条事件和唯一终态，Run/Thread/Protocol 均从 PG 回放。四文件定向回归 101 passed、4 skipped；平台三个定向文件共 39 项 unittest 通过。仍缺平台 API 联合撤权与跨用户重连、JS SDK 和候选双 API/worker 的其他故障路径；状态 **partial**。
- [ ] **T05 容量与上线验收：** 位置：`docs/compatibility-profile.md`、`docs/production-runbook.md`、`verification.md` 和平台网关回归；预期：候选双包可安装运行，GraphHarbor/runtime-service/平台 API 的 HTTP/worker/数据库链路通过，稳定容量和回退可复现。2026-09-25 隔离库同 fixture 写入 10,000 条 2.557s（重写 2.623s），清理 10,000 条 0.398s；活跃行降至 57，PG 表总大小仍约 20 MB、死元组 20,000，须靠 vacuum 后续回收。PG17 备份已完整恢复到独立库且仍有 10,000 条原始事件；此前真实 worker 自动清理另一个 10,000 条批次已验证。旧 `dist/` runtime wheel 缺迁移 009，已从当前源码重新构建、安装双包 0.13.0.post32 到 `/tmp/graphharbor-event-retention-candidate/install`；独立 `PYTHONPATH` 导入两个包、版本、清理方法和迁移 009 均成功。另建 `graphharbor_event_retention_candidate`，仅在该库运行候选 wheel 的 `graphharbor migrate upgrade` 到 009，并在 18631 端口运行候选 API/worker；worker 启动后 `probe_event_replay.py` 的 Run 初始/重连、Thread 全量/续传、Protocol 入口均收到回放帧，进程已停止。平台 Web 会话 20 项通过、真实 SDK 链路 1 项因缺联调环境跳过。独立平台库与候选 Runtime 联调完成网关 Thread 创建、读取、state/history 200，未登录 401；平台角色签发修复后 Graph 搜索 200。稳定期容量曲线与离线回退合并未验收；浏览器断线/HITL/文件操作不属于本专项门槛；状态 **partial**。

  2026-09-25 补验：修复版 runtime wheel 与同版本 langhost wheel 安装至 `/tmp/graphharbor-event-retention-fixed/install` 后，独立候选 API/worker 的真实 Redis 断连恢复和两个 worker 进程竞争清理通过。验收图从本机 `miaomiaoai` 注入 `deepseek-v4.1-flash` 后，真实模型 Run 为 success，状态 `provider_streamed=true`、分块数 2，PG 13 条事件含唯一终态；只证明通用执行链路，不代替平台 runtime-service 的模型策略验收。稳定容量和离线回退合并仍缺，T05 保持未完成。
