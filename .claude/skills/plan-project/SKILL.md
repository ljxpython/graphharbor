---
name: plan-project
description: AI 判断改动为链路改动或治理改动时自动调用（不需要用户手动触发），在 docs/projects/{YYYYMMDD}-{项目名}/ 下创建项目目录和核心文档（概览、方案、任务、验证）。跳过条件：单项目改动、纯讨论/探索性问题不调用。
---

# Plan Project（规划项目）

用于规划新的项目或重构，创建项目文档结构。**这是 AI 在判断出改动级别后自动调用的 Skill，用户通常不需要手动触发**——判断标准见 `AGENTS.md` 的「改动分级」。

## 触发条件

- 链路改动：跨服务、影响契约或数据模型
- 治理改动：影响架构、安全、数据、生产环境
- 需要多人协作或跨会话的工作
- 需要评审的重要改动

## 跳过条件

- 单项目改动（单服务内部、不影响对外契约）——直接实现，不创建项目文档
- 纯讨论、探索性规划、"这个方案是否可行"类问题——直接对话给结论，不生成文档；用户明确要落地时再触发本 Skill

## 工作流

1. **确定项目信息**（AI 从对话上下文自行判断，只在真正不确定时才问用户）
   - 项目名称（kebab-case，如 `runtime-modeling-refactor`）
   - 项目目标（一句话描述）
   - 影响范围（哪些服务）
   - 改动级别（链路改动 或 治理改动）

2. **选择模板档位**（见下方「模板档位」，默认标准模板，满足升级条件才用多专题模板）

3. **创建项目目录、生成核心文档**（按选定档位，见下方对应模板）

## 模板档位

### 档位判断标准

- **默认：标准模板**——链路改动、大多数治理改动都用这个
- **升级：多专题模板**——满足以下任一条件时使用：
  - 方案规划过程中发现自然拆成 3 个以上互相独立、可以分别验收的子模块（比如"前端契约""后端契约""迁移方案""兼容性 profile"这种各自独立成篇的东西）
  - 治理改动且预计跨多周/多阶段执行，一开始就能预见会拆分成多个子专题
- 不确定选哪档时先用标准模板；规划过程中发现装不下再升级为多专题模板（把 `plan.md` 拆成 `01-xxx.md`/`02-xxx.md`，`tasks.md`/`verification.md` 的内容分别并入对应编号文档，不再维护这两个全局文件）
- 参考实例（不在 `docs/projects/` 下，仅作为多专题模板的写法参考）：`apps/runtime-service/docs/knowledge/platform-runtime-integration/`

### 标准模板

```
docs/projects/{YYYYMMDD}-{项目名}/
├── README.md           # 项目概览
├── plan.md           # 整体方案
├── tasks.md           # 任务拆分
├── verification.md           # 验证计划
└── implementation/             # 实现细节（后续创建）
```

### 多专题模板

```
docs/projects/{YYYYMMDD}-{项目名}/
├── README.md              # 导航 + 总体状态，含"阅读顺序"列表
├── 01-{子专题}.md          # 每个子专题自成一档：目标/方案设计/任务拆分/验证要求与记录/状态
├── 02-{子专题}.md
├── ...
└── implementation/         # 可选，更细粒度的逐次实现记录（跟子专题文档不是一回事）
```

每个编号文档内部结构统一：目标 → 方案设计 → 任务拆分 → 验证要求与记录 → 状态。单独看一个文档就能知道这块子专题干没干完，不用翻全局 `tasks.md`。README.md 的"状态"字段汇总所有子专题的完成度（例如："总体方案已确认；01/02 已完成；03 进行中；仅 04 待评审"这种粒度）。

## 标准模板 - 文档内容

### README.md 模板

```markdown
# {项目标题}

## 项目概述
- **时间：** {开始日期} 至 {预计结束日期}
- **目标：** {一句话描述}
- **负责人：** @{用户名}
- **状态：** 规划中 / 进行中 / done / partial / blocked / deferred（完成后用 `verify-change` 的四态判定，见该 Skill）

## 快速导航
- [整体方案](plan.md)
- [任务拆分](tasks.md)
- [验证记录](verification.md)

## 改动范围
- **影响服务：** {列出服务}
- **改动级别：** 链路改动 / 治理改动
- **预计工作量：** X 人天

## 关键决策
1. {决策1}
2. {决策2}
```

### plan.md 模板

```markdown
# {项目标题} - 整体方案

## 背景
{为什么要做这个项目}

## 目标
{要达成什么目标}

## 方案设计

### 整体架构
{架构说明}

### 关键改动点

#### 1. {改动点1}
- **文件：** `apps/xxx/src/xxx.py`
- **改动：** {说明}
- **理由：** {原因}

#### 2. {改动点2}
- **文件：** `apps/xxx/src/yyy.py`
- **改动：** {说明}
- **理由：** {原因}

### 技术选型
- **选型1：** {说明和理由}
- **选型2：** {说明和理由}

## 链路影响

### 受影响的调用链路
```
service-a → service-b → service-c
```

### 契约变更
- {契约1}: {变更说明}
- {契约2}: {变更说明}

## 风险和依赖
- **风险1：** {说明} → **应对：** {措施}
- **依赖1：** {说明}

## 实施计划
1. Phase 1: {阶段1}
2. Phase 2: {阶段2}
3. Phase 3: {阶段3}
```

### tasks.md 模板

```markdown
# {项目标题} - 任务拆分

## Phase 1: 基础建设

### Task 1.1: {任务名称}
- **文件：** `apps/xxx/src/xxx.py`
- **函数/类：** `ClassName`, `function_name()`
- **改动：** {具体改动说明}
- **预计：** 0.5天
- **状态：** 待开始

### Task 1.2: {任务名称}
- **文件：** `apps/xxx/src/yyy.py`
- **改动：** {具体改动说明}
- **预计：** 1天
- **状态：** 待开始

## Phase 2: 核心实现

### Task 2.1: {任务名称}
- **描述：** {说明}
- **状态：** 待开始

## Phase 3: 验证和优化

### Task 3.1: {任务名称}
- **描述：** {说明}
- **状态：** 待开始

## 进度追踪
- [ ] Phase 1 完成
- [ ] Phase 2 完成
- [ ] Phase 3 完成
```

### verification.md 模板

```markdown
# {项目标题} - 验证计划和记录

## 验证计划

### 单元测试
- [ ] `test_xxx()` - 测试 xxx 功能
- [ ] `test_yyy()` - 测试 yyy 功能

### 集成测试
- [ ] **场景1：** {说明}
  - 步骤：{操作步骤}
  - 预期：{预期结果}
  
- [ ] **场景2：** {说明}
  - 步骤：{操作步骤}
  - 预期：{预期结果}

### 端到端测试
- [ ] **链路1：** platform-web → platform-api → runtime-service
  - 操作：{用户操作}
  - 验证点：{关键验证点}

### 性能测试
- [ ] {性能指标} - 目标：{目标值}

## 验证记录

### {日期} 验证
**执行人：** @{用户名}

#### 单元测试
- ✅ test_xxx - 通过
- ✅ test_yyy - 通过
- ⚠️ test_zzz - 失败，原因：{说明}

#### 集成测试
- ✅ 场景1 - 通过
- ❌ 场景2 - 失败，问题：{说明}

#### 端到端测试
- ✅ 链路1 - 通过，耗时 2.3s

#### 问题和修复
1. **问题：** {问题描述}
   - **原因：** {原因分析}
   - **修复：** {修复方案}

#### 最终结论
✅ 验证通过 / ⚠️ 部分通过 / ❌ 未通过

{详细说明}
```

## 多专题模板 - 文档内容

### README.md 模板

```markdown
# {项目标题}

## 项目概述
- **时间：** {开始日期} 至 {预计结束日期}
- **目标：** {一句话描述}
- **负责人：** @{用户名}
- **状态：** {汇总所有子专题完成度，例如："总体方案已确认；01/02 已完成；03 进行中；仅 04 待评审"}

## 阅读顺序
1. [{子专题1标题}](01-{子专题}.md)：{一句话说明这篇讲什么}
2. [{子专题2标题}](02-{子专题}.md)：{一句话说明这篇讲什么}
3. ...

## 改动范围
- **影响服务：** {列出服务}
- **改动级别：** 链路改动 / 治理改动
- **预计工作量：** X 人天

## 关键决策
1. {决策1}
2. {决策2}
```

### 每个子专题文档（`01-{子专题}.md` 等）模板

```markdown
# {子专题标题}

## 目标
{这个子专题要解决什么，为什么要拆成独立一篇}

## 方案设计
{架构说明、关键改动点、技术选型——写法同标准模板 plan.md 的对应部分}

## 任务拆分
- [ ] Task 1：{说明} - **文件：** `apps/xxx/...` - **状态：** 待开始
- [ ] Task 2：{说明} - **状态：** 待开始

## 验证要求与记录
### 验证要求
- [ ] {验证项1}

### 验证记录
#### {日期} 验证
- ✅/⚠️/❌ {结果说明}

## 状态
规划中 / 进行中 / done / partial / blocked / deferred（四态判定见 `verify-change`）
```

## 输出示例

标准模板创建完成后输出：

```
✅ 项目文档已创建：docs/projects/20260908-runtime-modeling-refactor/

📁 文件列表：
  - README.md         项目概览
  - plan.md         整体方案
  - tasks.md         任务拆分
  - verification.md         验证计划

📝 下一步：
1. 完善 plan.md 中的方案设计
2. 细化 tasks.md 中的任务拆分
3. AI 接下来自动调用 implement-feature 开始实现
```

多专题模板创建完成后输出：

```
✅ 项目文档已创建：docs/projects/20260908-agent-runtime-overhaul/（多专题模板）

📁 文件列表：
  - README.md         导航 + 总体状态
  - 01-xxx.md          子专题1
  - 02-xxx.md          子专题2

📝 下一步：
1. 完善 README.md 的阅读顺序和各子专题一句话说明
2. 逐篇细化各子专题文档的方案设计和任务拆分
3. AI 接下来自动调用 implement-feature 开始实现
```

## 注意事项

- 项目名称使用 kebab-case，如 `runtime-modeling-refactor`
- 文档内容要具体，不要空洞
- 文件和函数要写清楚，便于后续查找
- 验证计划要明确，可执行
- 对于治理改动，创建后需要评审才能开始实现
- 规划中途发现标准模板装不下（子模块自然拆成 3 个以上），随时可以升级为多专题模板，把已有内容拆分迁移过去，不用推翻重来
